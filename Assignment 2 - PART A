Part A
------

***What will the following commands do?***

a) echo "Hello,World!"
-> Output:
Hello,World!

b) name="Productive"
-> Creates variable "name" and adds "Productive" as value.

c) touch file.txt
-> Creates file.txt file

d) ls -a
-> List all files including hidden ones

e) rm file.txt
-> Deletes file.txt file

f) cp file1.txt file2.txt
-> Creates file2.txt if not exists and copies data from file1.txt into file2.txt. If file2.txt exists, it will overwrite the data in file2.txt

g) mv file.txt /path/to/directory/
-> Moves file.txt to particular path or destination.

h) chmod 755 script.sh
-> Changes permissions for script.sh, 7 gives owner permissions to read, write and execute.5 gives group and other user permissions to only read and execute.

i) grep "pattern" file.txt
-> Searches and prints "pattern" word from file.txt

j) kill PID
-> Use to kill process through PID

k) mkdir mydir && cd mydir && touch file.txt && echo "Hello, World!" > file.txt && cat file.txt
-> Creaates mydir directory. Navigates to mydir. Creates file.txt in mydir. Prints Hello, World! in file.txt, Opens file.txt

l) ls -l | grep ".txt"
-> Searches .txt files in directory and also shows it's owner and permissions.

m) cat file1.txt file2.txt | sort | uniq
-> Opens file1.txt and file2.txt and also sort them for only unique entries.

n) ls -l | grep "^d"
-> Searches for only directory and prints them with permissions. 

o) grep -r "pattern" /path/to/directory/
-> Searches for pattern in particular directory and its subdirectories.

p) cat file1.txt file2.txt | sort | uniq
-> Opens, sorts and prints only duplicate lines from file1.txt and file2.txt

q) chmod 644 file.txt
-> Set permissions as read, write for owner and only read for other users and group for file.txt

r) cp -r source_directory destination_directory
-> Copies a directory completely including subdirectories to destination directory.

s) find /path/to/search -name "*.txt"
-> Searches for .txt extension files in particular path

t) chmod u+x file.txt
-> Gives owner permission to execute in file.txt

u) echo $PATH
-> Prints all executable file paths.
